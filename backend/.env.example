# ===================================================
# Fichier d'exemple pour les variables d'environnement
# ===================================================
#
# Copiez ce fichier et renommez-le en ".env" dans le même dossier (`backend/`).
# Remplissez ensuite les variables avec vos propres valeurs.

# --- Base de Données (Obligatoire pour la production) ---
# L'URL complète de votre base de données PostgreSQL (fournie par Supabase ou autre).
# Si cette variable n'est pas définie, l'application utilisera une base de données SQLite locale.
DATABASE_URL=postgresql://user:password@host:port/dbname

# --- Configuration du Frontend (Obligatoire pour la production) ---
# Une liste d'URL, séparées par des virgules, qui sont autorisées à faire des requêtes à ce backend.
# Mettez ici l'URL de votre portfolio et de votre interface d'administration une fois déployés.
# Pour le développement local, vous pouvez laisser cette variable vide.
FRONTEND_ORIGINS=https://votre-domaine-frontend.com,https://votre-domaine-admin.com

# --- Configuration des E-mails (Optionnel) ---
# Remplissez ces variables si vous voulez que le formulaire de contact fonctionne.
MAIL_SERVER=smtp.example.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=votre-email@example.com
MAIL_PASSWORD=votre-mot-de-passe
MAIL_DEFAULT_SENDER=contact@example.com
